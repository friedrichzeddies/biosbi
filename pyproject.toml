[project]
name = "biosbi"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "fastapi",
    "httpx",
    "markdown",
    "pyyaml",
    "torch",
    "sbi",
    "pydantic",
    "uvicorn",
    "jinja2>=3.1.6",
    "python-multipart>=0.0.22",
]
